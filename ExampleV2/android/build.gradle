buildscript {
    ext {
        // buildToolsVersion = "34.0.0"
        // minSdkVersion = 23
        // compileSdkVersion = 34
        // targetSdkVersion = 34
        // ndkVersion = "26.1.10909125"
        // kotlinVersion = "1.9.24"
        buildToolsVersion = "35.0.0" // Updated to match Android 15 (API level 35)
        minSdkVersion = 23
        compileSdkVersion = 35 // Updated to match Android 15 (API level 35)
        targetSdkVersion = 35 // Updated to match Android 15 (API level 35)
        ndkVersion = "26.1.10909125"
        kotlinVersion = "1.9.24"
    }
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        classpath("com.android.tools.build:gradle")
        classpath("com.facebook.react:react-native-gradle-plugin")
        classpath("org.jetbrains.kotlin:kotlin-gradle-plugin")
    }
}

apply plugin: "com.facebook.react.rootproject"
